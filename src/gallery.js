import React from "react";
import { postings as postings} from "./postings";

function Gallery () {
    return(
        <div>
            postings.map((posting)=>{

            })
        </div>
    )
}

export default Gallery;